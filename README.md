# valrun-portal

The crossroads / landing page for **valrun.org** — gateways to the Val'Run family:
Codex (codex.valrun.org), Chronicles (stories.valrun.org), Tabletop (app.valrun.org).

Static single page. No build step — deploy `index.html` + `favicon.svg` as-is
(Cloudflare Pages/Workers: framework **None**, build command empty, output dir `/`).
